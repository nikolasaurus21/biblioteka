@using PagedList;
@using PagedList.Mvc;
@model IEnumerable<Biblioteka.DTO.NajnovijaIzdanjaDTO>

@{
    ViewBag.Title = "ListaIzdanja";
}
@*<div id="preloader" class="d-flex justify-content-center align-items-center" style="position: fixed; top: 0; left: 0; z-index: 9999; width: 100%; height: 100%; background-color: rgba(0,0,0,0.5);">
    <div class="spinner-border text-primary" role="status">
        <span class="visually-hidden">Loading...</span>
    </div>
</div>*@

<h2 class="navbar-text text-center">Najnovija izdanja</h2>


<div class="table-responsive">
    @Html.ActionLink("Nazad", "Index", "Home", null, new { @class = "btn btn-dark mb-2" })

    <table id="izdanja" class="table table-striped table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>Knjiga</th>
                <th>Godina izdanja</th>
                <th>Izdavačka kuća</th>
                <th>Izdatih</th>
                <th>Na stanju</th>
                <th>Slika</th>
            </tr>
        </thead>

        <tbody>

            @foreach (var izdanje in Model)
             {
                <tr accesskey="@izdanje.IzdanjeID">
                    <td>@izdanje.Knjiga</td>
                    <td>@izdanje.Godina</td>
                    <td>@izdanje.IzdavackaKuca</td>
                    <td>@izdanje.BrojIzdatih</td>
                    <td>@izdanje.BrojNaStanju</td>
                    <td><img src="@izdanje.SlikaKorica" alt="Slika" class="img-thumbnail" width="100" height="100"></td>
                </tr>
             }
        </tbody>
    </table>

    <div class="d-flex justify-content-center">
        <nav aria-label="Page navigation">
            <ul id="pagination" class="pagination ">
            </ul>
        </nav>
    </div>




    @*<div class="d-flex justify-content-center">
            @Html.PagedListPager((IPagedList)Model, page => Url.Action("ListaIzdanja", new { page }),
                new PagedListRenderOptions
                {
                    //odje moze da se dodaju neke sitnice ako treba ali ja necu nista



                })
        </div>*@




</div>
@section Scripts {
    <script>
        //$(document).ready(function () {
        //    // Dodaje Bootstrap klasu 'btn-dark' i margine na sve elemente unutar .pagination
        //    $(".pagination li a").addClass("btn btn-dark m-1");

        //    // Promeni boju za trenutno aktivno dugme
        //    $(".pagination li.active a").removeClass("btn-dark").addClass("btn-success");
        //});

        //$(window).on('load', function () {
        //    console.log('Window is loaded, should remove preloader now.');
        //    $('#preloader').fadeOut('slow', function () {
        //        console.log('Preloader removed.');
        //    });
        //});




        let currentPage = 1;
        let rowsPerPage = 7;

        $(document).ready(function () {
            const rows = $('#izdanja tbody tr');
            const totalPages = Math.ceil(rows.length / rowsPerPage);

            function displayRows() {
                rows.hide().slice((currentPage - 1) * rowsPerPage, currentPage * rowsPerPage).show();
            }

            function displayPagination() {
                $('#pagination').empty();
                $('#pagination').append(`<button class="btn btn-sm m-1 btn-dark prev" ${currentPage === 1 ? 'disabled' : ''}>&lt;&lt; Prethodna</button>`);

                for (let i = 1; i <= totalPages; i++) {
                    const activeClass = i === currentPage ? 'btn-dark' : 'btn-secondary';
                    $('#pagination').append(`<button class="btn btn-sm m-1 ${activeClass}" data-page="${i}">${i}</button>`);
                }

                $('#pagination').append(`<button class="btn btn-sm m-1 btn-dark next" ${currentPage === totalPages ? 'disabled' : ''}>Sledeća &gt;&gt;</button>`);
            }

            $('#pagination').on('click', 'button', function (e) {
                e.preventDefault();
               
                if ($(this).hasClass('prev')) {
                    currentPage = Math.max(currentPage - 1, 1);
                } else if ($(this).hasClass('next')) {
                    currentPage = Math.min(currentPage + 1, totalPages);
                } else {
                    currentPage = +$(this).data('page');
                }
                displayRows();
                displayPagination();
                
            });

            displayRows();
            displayPagination();
        });








    </script>
}








